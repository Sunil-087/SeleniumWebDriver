package day30;

public class FramesConcept {
	/*
	frames/iframes
	--------------
	To access the element, we need to go into the frame, To go below are the methods:
	
	driver.swithTo().frame(name)
	driver.switchTo().frame(id)
	driver.switchTo().frame(WebElement)
	
	driver.switchTo().defaultContent(); //go back to page
	
	
	3 types of switching methods
	--------------------------
	1) browser windows = driver.switchTo().window(windowID/window handle)
	2) alert = driver.switchTo().alert()
			   driver.swithcTo().accept()
			   driver.swithcTo().dismiss()
	3) frames
	driver.switchTo().frame(id)
	driver.switchTo().frame(name)
	driver.swithcTo().frame(WebElement)
	
	
	
	
	
	
	
	
	
	
	
	
*/
}
